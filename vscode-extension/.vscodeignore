src/**
dist-types/**
scripts/**
**/*.map
tsconfig.json
esbuild.*.js
node_modules/**
# The vsix BUNDLES the latexml-oxide LSP engine (bin/latexml_oxide):
# single artifact, extension<->engine version-locked (rust-analyzer model).
# Everything else in bin/ stays out.
bin/**
!bin/latexml_oxide
media/preview.js.map
